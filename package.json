{
  "name": "state-logger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reduxlogger": "remotedev --hostname=localhost --port=3001"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "remotedev-server": "^0.3.1"
  }
}
